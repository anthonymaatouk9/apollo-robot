{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -m waitress --host=0.0.0.0 --port=$PORT app_public:app",
    "healthcheckPath": "/about",
    "restartPolicyType": "ON_FAILURE"
  }
}
